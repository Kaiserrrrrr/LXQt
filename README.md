# 🪽 LXQt 
Another way to automate LXQt for Arch Linux
## 🚀 Installation
```bash
archinstall && arch-chroot /mnt /bin/bash -c "$(curl -fsSL https://cdn.jsdelivr.net/gh/Kaiserrrrrr/LXQt/install.sh)" && reboot
```
